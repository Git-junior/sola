<div class="card">
  <%= link_to image_tag(photo.image, class: :card__img ), photo_path(photo.id)%>
  <div class="card__body">
    <%= link_to photo.content, photo_path(photo.id), class: :card__title%>
  </div>
  <div class="card__date">
    <%= l photo.created_at %>
  </div>
</div>